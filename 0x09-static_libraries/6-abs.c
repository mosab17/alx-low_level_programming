#include "main.h"

/**
 * _abs - Computers the absolute
 *
 * @i: input as an integer
 *
 *
 *
 * Return: absloue value
*/

int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}

